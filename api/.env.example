# Copy to .env and fill in. For local dev these come from `supabase status`
# (run from BE/) once `supabase start` is running.

DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
SUPABASE_URL=""
PORT=3000
BOOTSTRAP_SUPERADMIN_EMAIL=""
